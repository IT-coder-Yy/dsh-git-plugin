# 贡献指南

感谢参与改进。请保持改动小而清晰，并为行为变化补充测试。

## 开发流程

1. 使用 Node.js `^22.19.0 || >=24.0.0` 和 Git。
2. Fork 仓库并从最新主分支创建功能分支。
3. 修改前先说明问题、预期行为和安全影响。
4. 运行 `npm run check`；涉及执行或验证逻辑时，补充真实临时仓库集成测试。
5. 提交 Pull Request，描述改动、验证结果和兼容性影响。

本项目没有运行时 npm 依赖。请避免为了简单逻辑引入新依赖；确有必要时，在 Pull Request 中解释收益、维护成本和供应链风险。

## 代码约定

- 保持 CommonJS 和现有静态 Cordis 插件结构。
- 不绕过 `validateCommand` 或 `executeRegisteredProposal` 执行模型提供的命令。
- 新增 Git 子命令或选项前，必须检查其外部程序、hook、helper 和破坏性行为。
- 错误和诊断信息不得泄露凭据。
- 不提交真实令牌、私有仓库地址或个人配置。

安全漏洞请按 [SECURITY.md](SECURITY.md) 私下报告，不要提交公开 Issue。
